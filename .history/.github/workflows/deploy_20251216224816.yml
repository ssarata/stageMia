name: Deploy React + Vite

on:
  push:
    branches:
      - main  # Production seulement après merge
      - dev   # Preview à chaque push pendant le dev
permissions: 
  contents: write

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js 
        uses: actions/setup-node@v3
        with:
          node-version: 22

      - name: Install Dependencies
        run: npm install

      # Optionnel : Lancer les tests avant de déployer
      # - name: Run tests
      #   run: npm test

      - name: Build project
        run: npm run build 

      # 🟢 DEV : Déploiement Preview (à chaque push sur dev)
      # - name: Deploy to Vercel Preview
      #   if: github.ref == 'refs/heads/dev'
      #   env:
      #     VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      #   run: npx vercel --token $VERCEL_TOKEN
        
      # 🔴 MAIN : Déploiement Production (seulement après merge)
      # - name: Deploy to Vercel Production
      #   if: github.ref == 'refs/heads/main'
      #   env:
      #     VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      #     VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      #     VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
      #   run: npx vercel --prod --token $VERCEL_TOKEN --yes
